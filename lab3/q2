#include<stdio.h>
#include<math.h>
void main()
{
double x, y;
printf("enter x please:");
scanf("%lf", &x);
y = x + pow(1.0 / 3 * pow(x, 4) + 2 * pow(x, 0.5), 0.2);
printf("x+((1/3)*x^4+2*x^0.5)^1/5 = %g + ((1/3)*%g^4+2*%g^0.5)^2=%g", x, x, x, y);
}
